# Phone must reach this URL (same Wi‑Fi). Use ipconfig → IPv4, NOT localhost.
# Rebuild the app after changing (EAS: set same vars under Project → Environment variables).
EXPO_PUBLIC_OTP_API_BASE_URL=http://192.168.1.10:4000
EXPO_PUBLIC_ADMIN_EMAIL=mpwatersupply.mandya@gmail.com
